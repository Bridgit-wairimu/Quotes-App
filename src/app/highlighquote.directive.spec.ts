import { HighlighquoteDirective } from './highlighquote.directive';

describe('HighlighquoteDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlighquoteDirective();
    expect(directive).toBeTruthy();
  });
});
